# WARNING: Automatically generated file using BAF (Build system Agnostic File) tooling (version master)
# WARNING: You should contribute changes to https://gitlab.com/prpl-foundation/components/core/plugins/ssh-server/-/blob/main/baf.yml instead
config PACKAGE_ssh-server
    tristate
    select SAH_AMX_SSH_SERVER

if PACKAGE_ssh-server

menu "Select ssh-server build options"
    depends on PACKAGE_ssh-server

config SAH_AMX_SSH_SERVER
    bool "Build ssh server plugin"
    default y

config SAH_AMX_SSH_SERVER_START_ORDER
    int "start up order of ssh server plugin"
    default 50

config SAH_AMX_SSH_SERVER_STOP_ORDER
    int "shutdown order of ssh server plugin"
    default 50

config SAH_AMX_SSH_SERVER_SOP
    bool "Build ssh server plugin for SOP"
    default n

endmenu

endif
